# Production: set this to your deployed backend's URL, including /api
# Example: https://reconciliation-system-production.up.railway.app/api
#
# Local dev: leave empty — Vite's proxy in vite.config.js forwards /api → :4000
VITE_API_URL=
